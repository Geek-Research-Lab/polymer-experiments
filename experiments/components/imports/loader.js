function loaderMix(gems) {
	'use strict';

	// importing
	var xhr = ['xhr.js'];
	xhr = xhr.gems;
	var flags = gems.flags;

	// main
	var loader = function(onLoad, onComplete) {
		//
		// Still more to code!
		//
	};
}
